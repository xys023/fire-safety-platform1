# 智慧消防联防联控平台 - 部署教程

> 本教程面向**编程零基础**的用户，按照步骤操作即可成功部署。

---

## 一、项目简介

本项目包含三部分：

| 模块 | 访问地址 | 说明 |
|------|---------|------|
| PC端管理后台 | `http://localhost:3000/admin/` | 管理员使用，数据大屏、场所/设备/隐患/警情管理 |
| 移动端小程序 | `http://localhost:3000/mobile/` | 网格员/业主使用，手机浏览器访问，可封装为微信小程序 |
| 后端API服务 | `http://localhost:3000/api/` | 数据服务，自动启动，无需单独操作 |

**技术栈：** Node.js + Express + SQLite（sql.js 纯JS版，零编译依赖）+ Vue 3 + Element Plus + ECharts

**系统要求：** Windows 7 及以上 / Mac OS 10.12 及以上 / Ubuntu 18.04 及以上

> **重要说明：** 本项目使用 sql.js（纯 JavaScript/WebAssembly 版 SQLite），**无需安装 Python、Visual Studio 或任何编译工具**，任何 Node.js 版本（16.x ~ 25.x）均可直接运行，开箱即用。

---

## 二、安装 Node.js（只需安装一次）

### Windows 系统

1. 打开浏览器，访问 Node.js 官网：**https://nodejs.org/**
2. 点击首页的 **LTS（长期支持版）** 下载按钮（推荐 18.x 或 20.x 版本）
3. 双击下载的 `.msi` 安装包
4. 一路点击 **Next（下一步）**，安装路径默认即可（**注意：安装路径不要有中文和空格**）
5. 安装完成后，按 `Win + R`，输入 `cmd`，回车打开命令提示符
6. 输入以下命令验证安装：
   ```
   node -v
   npm -v
   ```
   如果显示版本号（如 `v18.19.0`），说明安装成功

### Mac 系统

1. 访问 **https://nodejs.org/**，下载 LTS 版本的 `.pkg` 安装包
2. 双击安装，一路继续即可
3. 打开"终端"（Terminal），输入验证：
   ```bash
   node -v
   npm -v
   ```

### Linux（Ubuntu/Debian）

```bash
# 方法一：使用官方源安装
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 验证
node -v
npm -v
```

---

## 三、启动项目

### 方法一：双击启动（最简单，推荐新手）

#### Windows 用户
1. 将项目文件夹放到一个**没有中文和空格**的路径下（例如 `D:\fire-safety-platform`）
2. 双击项目文件夹中的 **`start.bat`** 文件
3. 首次启动会自动安装依赖包（约需1-3分钟），请耐心等待
4. 看到"启动成功"提示后，打开浏览器访问即可

#### Mac/Linux 用户
1. 打开终端，进入项目目录：
   ```bash
   cd /path/to/fire-safety-platform
   ```
2. 添加执行权限（只需一次）：
   ```bash
   chmod +x start.sh
   ```
3. 启动：
   ```bash
   ./start.sh
   ```

### 方法二：命令行启动

```bash
# 1. 进入项目目录
cd fire-safety-platform

# 2. 安装依赖（首次运行需要，以后不需要）
npm install

# 3. 启动服务
node server.js
```

启动成功后会显示：
```
========================================
  智慧消防联防联控平台启动成功！
  PC端后台：http://localhost:3000/admin/
  移动端：  http://localhost:3000/mobile/
  API文档： http://localhost:3000/api/health
========================================
```

---

## 四、访问系统

### PC端管理后台
1. 打开浏览器（推荐 Chrome / Edge）
2. 地址栏输入：**http://localhost:3000/admin/**
3. 使用以下账号登录：

| 角色 | 账号 | 密码 |
|------|------|------|
| 管理员 | admin | 123456 |
| 网格员 | grid01 | 123456 |
| 业主 | owner01 | 123456 |
| 运维人员 | operator01 | 123456 |
| 消防部门 | fire01 | 123456 |

### 移动端小程序
1. 在手机浏览器中输入：**http://你的电脑IP:3000/mobile/**
   - 查看电脑IP：Windows 输入 `ipconfig`，Mac/Linux 输入 `ifconfig`
   - 手机和电脑需在**同一个WiFi网络**下
2. 也可以在电脑浏览器中按 F12，点击手机图标，模拟手机访问
3. 推荐使用网格员账号 `grid01 / 123456` 登录体验

---

## 五、常见问题

### Q1：双击 start.bat 闪退
**原因：** 未安装 Node.js 或安装路径有问题。
**解决：** 重新安装 Node.js，安装路径保持默认（`C:\Program Files\nodejs\`）。

### Q2：提示"npm 不是内部或外部命令"
**原因：** Node.js 环境变量未生效。
**解决：** 重启电脑后再试；或重新安装 Node.js。

### Q3：提示端口 3000 被占用
**解决：**
- 方法一：关闭占用 3000 端口的程序
- 方法二：修改项目端口：
  1. 用记事本打开项目根目录的 `server.js`
  2. 找到第17行 `const PORT = process.env.PORT || 3000;`
  3. 把 `3000` 改成其他端口（如 `8080`）
  4. 保存后重新启动

### Q4：手机访问不了移动端
**解决：**
1. 确保手机和电脑连的是同一个 WiFi
2. 检查电脑防火墙是否放行了 3000 端口
3. Windows 防火墙设置：控制面板 → Windows Defender 防火墙 → 允许应用通过防火墙 → 勾选 Node.js
4. 确认使用的是电脑的局域网 IP（不是 localhost）

### Q5：忘记管理员密码
**解决：** 管理员默认密码为 `123456`。如果修改后忘记，可以：
1. 登录 PC 后台 → 用户管理 → 找到对应用户 → 点击"重置密码"
2. 或删除 `data/fire_safety.db` 文件后重启（会重置所有数据）

### Q6：如何停止服务？
在启动服务的命令行窗口中按 `Ctrl + C`，然后输入 `Y` 确认。

---

## 六、数据备份

系统使用 SQLite 数据库，所有数据存储在一个文件中：

```
项目目录/data/fire_safety.db
```

**备份方法：** 直接复制 `fire_safety.db` 文件到其他位置即可。
**恢复方法：** 将备份的文件覆盖回 `data/` 目录，重启服务。

建议定期备份，特别是升级系统前。

---

## 七、生产环境部署（可选）

如果需要在公网服务器上长期运行，建议使用 PM2 进程管理：

```bash
# 1. 安装 PM2
npm install -g pm2

# 2. 启动服务
pm2 start server.js --name fire-platform

# 3. 设置开机自启
pm2 startup
pm2 save

# 常用命令
pm2 status          # 查看状态
pm2 logs fire-platform  # 查看日志
pm2 restart fire-platform  # 重启
pm2 stop fire-platform     # 停止
```

**Nginx 反向代理配置（可选）：**

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # PC端后台
    location /admin/ {
        proxy_pass http://127.0.0.1:3000/admin/;
    }

    # 移动端
    location /mobile/ {
        proxy_pass http://127.0.0.1:3000/mobile/;
    }

    # API
    location /api/ {
        proxy_pass http://127.0.0.1:3000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 上传文件
    location /uploads/ {
        proxy_pass http://127.0.0.1:3000/uploads/;
    }
}
```

---

## 八、封装为微信小程序（可选）

本项目移动端采用 H5 技术开发，可以通过以下方式封装为微信小程序：

### 方法一：使用 web-view 组件（最简单）

1. 在微信公众平台注册小程序账号
2. 下载微信开发者工具
3. 创建小程序项目，在页面中使用 web-view 组件：
   ```html
   <web-view src="https://your-domain.com/mobile/"></web-view>
   ```
4. 配置业务域名（需要 HTTPS）
5. 提交审核发布

### 方法二：使用 uni-app 重写（推荐长期方案）

将 H5 页面用 uni-app 框架重写，可同时发布到微信小程序、支付宝小程序、App 等多个平台。后端 API 无需修改。

---

## 九、安全建议

1. **修改默认密码**：首次登录后请立即修改 admin 密码
2. **修改 JWT 密钥**：在 `server.js` 中修改 `JWT_SECRET`（生产环境必须修改）
3. **使用 HTTPS**：生产环境配置 SSL 证书
4. **定期备份数据库**
5. **限制文件上传**：系统已限制仅允许图片格式，单文件不超过 10MB
6. **数据库参数化查询**：已全部使用参数化查询，防止 SQL 注入
7. **密码加密存储**：使用 bcrypt 加密，不明文存储

---

## 十、项目目录结构

```
fire-safety-platform/
├── server.js              # 服务入口
├── package.json           # 依赖配置
├── start.bat              # Windows 启动脚本
├── start.sh               # Mac/Linux 启动脚本
├── 部署教程.md             # 本文档
├── src/
│   ├── database.js        # 数据库初始化
│   ├── middleware/
│   │   └── auth.js        # JWT认证中间件
│   └── routes/            # API路由
│       ├── auth.js        # 登录注册
│       ├── users.js       # 用户管理
│       ├── grids.js       # 网格管理
│       ├── places.js      # 场所管理
│       ├── devices.js     # 设备管理
│       ├── alarms.js      # 警情管理
│       ├── hazards.js     # 隐患管理
│       ├── inspections.js # 巡查管理
│       ├── workorders.js  # 工单管理
│       ├── statistics.js  # 统计分析
│       ├── resources.js   # 消防资源
│       ├── knowledge.js   # 知识库
│       ├── notifications.js # 消息通知
│       └── upload.js      # 文件上传
├── public/
│   ├── admin/             # PC端后台
│   │   ├── index.html
│   │   └── js/
│   └── mobile/            # 移动端小程序
│       ├── index.html
│       ├── css/style.css
│       └── js/app.js
├── data/                  # 数据库文件（自动生成）
└── uploads/               # 上传文件目录（自动生成）
```

---

## 技术支持

如遇到问题，请检查：
1. Node.js 版本是否为 16 以上
2. 3000 端口是否被占用
3. 防火墙是否放行
4. 查看命令行窗口的错误信息
